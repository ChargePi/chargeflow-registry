name: "Build and push services"
on:
  push:
    branches:
      - main
    tags:
      - v*
    paths-ignore:
      - "api/**"
      - "docs/**"
      - "deployments/**"
      - "features/**"
      - "scripts/**"
      - "*.md"

  pull_request:
    types: [ opened, synchronize ]
    branches:
      - main
    paths-ignore:
      - "api/**"
      - "docs/**"
      - "deployments/**"
      - "features/**"
      - "scripts/**"
      - "*.md"

  workflow_dispatch:

jobs:
  build-chargeflow-registry:
    name: "Build and Push chargeflow-registry"
    uses: ./.github/workflows/build.yaml
    permissions:
      contents: read
      pull-requests: write
    with:
      service: chargeflow-registry
      push: ${{ github.event_name != 'pull_request' }}
      repository: chargeflow-registry
      dockerfile: "./build/Dockerfile"
    secrets: inherit